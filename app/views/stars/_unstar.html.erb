<div><%= form_for(micropost.stars.build, remote: true) do |f| %>
  <%= hidden_field_tag :micropost_id, micropost.id %>
  <%= image_submit_tag "unstar.png", class: "star" %>
<% end %>
</div>
<div><%= stars_counter(micropost) %></div>
